name: ucode2019
description: uCode 2019 by adidas
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  geolocator: ^3.0.1
  flutter_map: ^0.5.0
  sensors: ^0.4.0+1
  flutter_range_slider: ^1.1.0
  geo: ^0.2.0
  firebase_database: ^2.0.2
  firebase_core: ^0.3.0
  flutter_compass: ^0.1.0
  http: ^0.12.0+1
  #vibration: 1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:


  uses-material-design: true